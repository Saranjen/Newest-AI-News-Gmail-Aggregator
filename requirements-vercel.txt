# Minimal deps for FastAPI on Vercel only (subscribe / demo / health).
# Full pipeline (scrapers, Docling, etc.) uses requirements.txt — do not merge those here.
fastapi>=0.115.0
email-validator>=2.0.0
markdown>=3.7.0
openai>=2.7.2
psycopg2-binary>=2.9.11
pydantic>=2.0.0
python-dotenv>=1.2.1
sqlalchemy>=2.0.44

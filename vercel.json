{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pip install -r requirements-vercel.txt && cd frontend && npm install",
  "buildCommand": "cd frontend && VERCEL=1 npm run build"
}
